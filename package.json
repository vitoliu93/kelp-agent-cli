{
  "name": "myagent",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.ts",
    "build:bin": "bun build --compile src/index.ts --outfile dist/kelp && mkdir -p \"$HOME/.local/bin\" && ln -sf \"$PWD/dist/kelp\" \"$HOME/.local/bin/kelp\"",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.78.0",
    "@inquirer/prompts": "^8.0.1",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "consola": "^3.4.2"
  }
}
